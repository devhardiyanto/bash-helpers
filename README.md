# Install PHP

```bash
sudo apt install php7.4 libapache2-mod-php7.4 php7.4-{cli,fpm,dom,common,mysql,zip,gd,mbstring,curl,xml,bcmath,tokenizer,xmlrpc,pgsql}
```

```bash
sudo apt install php8.3 libapache2-mod-php8.3 php8.3-{cli,fpm,dom,common,mysql,zip,gd,mbstring,curl,xml,bcmath,tokenizer,xmlrpc,pgsql}
```
